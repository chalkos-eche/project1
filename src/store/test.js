const { create } = require('zustand');

// export const useCartStore = create(set => ({
// 	defaultCart: [],
// }));
// const useTestStore = create(set => ({
// 	initialValue: [],
// 	initialfunction(value) {
// 		set();
// 	},
// }));

// zustand 예제

// const useMemoStore = create(set => ({
// 	memoList: [],
// 	addMemo(value) {
// 		set(state => ({
// 			memoList: [...state.memoList, { contents: value, id: Date.now() }],
// 		}));
// 	},
// 	removeMemo(id) {
// 		c;
// 		set(state => ({
// 			memoList: state.memoList.filter(e => e.id !== id),
// 		}));
// 	},
// }));

// export default useMemoStore;
